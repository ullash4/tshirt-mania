import React from 'react';

const Uncle = ({house}) => {
    return (
        <div className='border-2 border-black m-5 p-5'>
            <h1>This is Uncle house {house}</h1>
        </div>
    );
};

export default Uncle;